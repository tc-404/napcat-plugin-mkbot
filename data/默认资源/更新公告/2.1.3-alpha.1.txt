2.1.3-alpha.1 [url:https://xunyunxiaoxiao.lanzouw.com/ixGkI3ivqvbg]
2026-02-21 11:55:00
【新增】【2】 第三方API接口(接口功能)
 
↓［详细数据］↓
 【1】
 - 搜 async function fetchAPI(url, method = "GET", data = null) {
 - 即可找到新增的便捷复制功能:访问其他API
 
 【2】
 - 搜 if(message == "菜单"){
 - 即可找到更改过的新菜单，同时更新了html文件
 
 - 搜 if(message == "接口功能" || message == "API接口"){
 - 即可找到新增的API接口菜单
 
 - 搜 if(message.match(/^(epic|Epic|EPIC)免费游戏$/)){
 - 即可找到新接入的免费接口:epic免费游戏，以合并消息转发输出
 
 - 搜 if(message.match(/^查(MC|mc|我的世界)服务器([\s\S]*)$/)){
 - 即可找到新接入的免费接口:查我的世界服务器状态，正常输出，有图标也会显示图标
 
 - 搜 if(message == "管理续火"){
 - 即可找到续火列表的展示
 
 - 搜 if(message.match(/^设置(群聊续火)内容([\s\S]*)$/)){
 - 这个就是更改每天续火内容的
 
 - 搜// ================== 定时任务 ==================
 - 再搜 // ================== 续火 ==================
 - 即可找到在定时任务中该功能的位置，其中如果「续火」成功执行，那么这个群就不会再支持一次「报时」，避免冲突